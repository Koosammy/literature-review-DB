# Core FastAPI and web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Database and ORM
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.12.0

# Authentication and security
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.1
python-multipart>=0.0.6

# Data validation and settings
pydantic[email]>=2.10.1
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Email and communication
fastapi-mail>=1.5.0
aiosmtplib>=3.0.2
email-validator>=2.2.0

# File handling and image processing
Pillow>=10.0.0
aiofiles>=23.0.0
python-multipart>=0.0.6

# Document processing
# PyMuPDF >=1.24 for Page.find_tables() (native table detection, no
# Ghostscript needed) and Page.cluster_drawings() (vector-figure detection).
PyMuPDF>=1.24.9
python-docx==1.1.0
pandas==2.1.4
matplotlib==3.8.2                    # Fallback table rendering when LibreOffice is unavailable for DOCX
openpyxl==3.1.2
pdfplumber>=0.9.0                    # Page-level fallback table detector
httpx>=0.25.0

# Note: DOCX table extraction gets pixel-accurate table crops by converting
# to PDF with a headless LibreOffice (`soffice`) and reusing the PDF engine.
# Install the `libreoffice` (or `libreoffice-writer`) system package for
# full DOCX table fidelity; without it, DOCX tables fall back to text-only
# extraction rendered with matplotlib.
